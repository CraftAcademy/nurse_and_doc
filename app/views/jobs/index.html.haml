.card
  .inner-card
    .card-title
      %h1 Jobs
    #list-item.flex
    - @jobs.each do |job|
      #hosp-name
        %p= job.name
      #hosp-button
        %button.button Remove
    .add-field
      %input.add-field-placeholder{placeholder: "Add new job"}
      %button.button Add