.card
  .inner-card
    .card-title
      %h1 Jobs
    #list-item.flex
    - @jobs.each do |job|
      #form-name
        %p= job.name
      #form-button
        %button.button Remove
    = link_to "Add New Job", new_job_path, class: "button", remote: true