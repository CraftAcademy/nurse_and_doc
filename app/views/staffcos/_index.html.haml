.card
  .inner-card
    .card-title
      %h1 Staffing Companies
    #list-item.flex
    - @staffcos.each do |staffcos|
      #form-name
        %p= staffcos.name
      #form-name
        %p= staffcos.email
      #form-button
        %button.button Remove
    = link_to "Add new company", new_staffco_path, class: "button", remote: true