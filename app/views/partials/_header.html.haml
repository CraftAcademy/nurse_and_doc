#header
  %h1.py-4 RSC Dashboard
  .zdh-button
    - if I18n.locale == I18n.default_locale
      = link_to "Svenska", locale: 'sv'
    - else
      = link_to "English", locale: 'en'
  .zdh-button
    = link_to "Create RSC account", new_user_path