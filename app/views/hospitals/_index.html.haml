.card
  .inner-card
    .card-title
      %h1 Hospitals 
    #list-item.flex
    - @hospitals.each do |hospital|
      #form-name
        %p= hospital.name
      #form-button
        %button.button Remove
    .add-field
      %input.add-field-placeholder{placeholder: "Add Hospitals"}
      = link_to t(:add), controller: :hospitals, action: :create, name: hospital.name, method: :post, class: 'header-button', remote: true
      -# = card.submit value: "Add", class: "bg-blue hover:bg-blue-dark text-white font-bold py-2 px-4 my-4 rounded focus:outline-none focus:shadow-outline"

